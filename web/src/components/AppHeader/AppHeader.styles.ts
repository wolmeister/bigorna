import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  logoContainer: {
    display: 'flex',
    height: '100%',
    alignItems: 'center',
  },
  group: {
    height: '100%',
  },
  title: {
    margin: 0,
  },
}));
