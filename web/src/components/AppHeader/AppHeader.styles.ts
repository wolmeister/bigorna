import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  logoContainer: {
    display: 'flex',
    height: '100%',
    alignItems: 'center',
    textDecoration: 'none',
    color: 'inherit',
  },
  group: {
    height: '100%',
  },
}));
